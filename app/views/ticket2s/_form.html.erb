<%= form_with(model: ticket2, local: true) do |form| %>
  <% if ticket2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket2.errors.count, "error") %> prohibited this ticket2 from being saved:</h2>

      <ul>
      <% ticket2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :tent %>
    <%= form.check_box :tent %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
